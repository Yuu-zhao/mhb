# GUI界面使用指南

## 启动GUI

```bash
python gui_app.py
```

或者使用启动脚本：
```bash
./run_gui.sh
```

## 界面功能说明

### 1. URL设置区域
- **网页地址输入框**：输入要抓取的网页URL
- 支持 `http://` 和 `https://` 协议

### 2. Cookie设置区域（可选）
- **Cookie字符串输入框**：粘贴从浏览器复制的Cookie
- **从文件加载Cookie**：从JSON文件加载Cookie
- **保存Cookie到文件**：将当前Cookie保存到文件，方便下次使用

#### 如何获取Cookie：
1. 在浏览器中打开目标网站并登录
2. 按 `F12` 打开开发者工具
3. 切换到 `Application`（应用程序）标签
4. 左侧选择 `Cookies` -> 选择网站域名
5. 复制所有Cookie的name和value

### 3. 抓取方法选择
- **Requests + Cookie**：快速方法，适合大多数情况（推荐）
- **Selenium**：支持JavaScript渲染，适合动态页面

### 4. 操作按钮
- **开始抓取**：开始抓取网页数据
- **保存到数据库**：将预览的数据保存到数据库（抓取成功后可用）
- **查看已保存数据**：查看数据库中所有已保存的数据
- **清空预览**：清空当前预览内容

### 5. 数据预览区域
- **标题预览**：显示抓取到的页面标题
- **内容预览**：显示抓取到的页面正文内容（可滚动查看）

### 6. 状态栏
- 显示当前操作状态和提示信息

## 使用流程

### 基本流程（无需登录）

1. 在"网页地址"输入框中输入URL
2. 选择抓取方法（默认使用Requests）
3. 点击"开始抓取"按钮
4. 等待抓取完成，在预览区域查看数据
5. 确认无误后，点击"保存到数据库"

### 需要登录的页面

1. **获取Cookie**（参考上面的"如何获取Cookie"）
2. 在"Cookie字符串"输入框中粘贴Cookie
3. 输入要抓取的URL
4. 选择抓取方法（Requests或Selenium）
5. 点击"开始抓取"
6. 查看预览数据
7. 点击"保存到数据库"

### 保存Cookie供以后使用

1. 输入Cookie后，点击"保存Cookie到文件"
2. 选择保存位置和文件名
3. 下次使用时，点击"从文件加载Cookie"即可

## 查看已保存的数据

1. 点击"查看已保存数据"按钮
2. 在弹出的窗口中查看数据列表
3. 双击某条数据可查看完整详情（包括URL、标题、内容和创建时间）

## 注意事项

1. **Cookie有效期**：Cookie可能会过期，过期后需要重新获取
2. **抓取时间**：Selenium方法比Requests方法慢，但支持JavaScript渲染
3. **内容长度**：如果内容过长，预览区域会截断显示，但完整内容会保存到数据库
4. **网络连接**：确保网络连接正常，某些网站可能需要VPN
5. **反爬虫**：如果遇到反爬虫限制，可以：
   - 使用Selenium方法
   - 更新Cookie
   - 添加延迟（需要修改代码）

## 常见问题

**Q: 点击"开始抓取"后没有反应？**  
A: 检查URL是否正确，网络是否正常。如果使用Cookie，检查Cookie是否有效。

**Q: 提示"抓取失败"？**  
A: 
- 检查URL是否正确
- 如果页面需要登录，确保Cookie正确
- 尝试使用Selenium方法
- 检查网络连接

**Q: 预览内容为空？**  
A: 可能是JavaScript渲染的页面，尝试使用Selenium方法。

**Q: 如何批量抓取多个URL？**  
A: 目前GUI不支持批量，可以编写脚本使用命令行工具批量处理。

**Q: 保存的数据在哪里？**  
A: 数据保存在SQLite数据库文件 `page_data.db` 中，可以通过"查看已保存数据"功能查看。

## 快捷键

- `Ctrl+C` / `Cmd+C`：复制选中文本
- `Ctrl+V` / `Cmd+V`：粘贴文本
- `Enter`：在输入框中按Enter可以快速开始抓取（需要先点击输入框）

# 多标签页抓取功能指南

## 功能概述

新增了**多标签页抓取**功能，可以自动依次点击页面上的不同标签页（人物/修炼、技能、道具/法宝），并提取每个标签页的关键信息。

## 使用方法

### Web界面

1. 输入要抓取的URL（必须是藏宝阁角色详情页）
2. 确保选择了 **Playwright** 抓取方法
3. 点击 **"多标签页抓取"** 按钮
4. 系统会自动：
   - 抓取初始页面（人物/修炼）的关键信息
   - 点击"技能"标签页，提取技能信息
   - 点击"道具/法宝"标签页，提取道具信息
5. 在预览区域查看提取的关键信息
6. 点击"保存到数据库"保存结构化数据

## 提取的关键信息

### 1. 人物/修炼（基础信息）
- 亮点、编号、卖家、卖家ID、是否上架、价格、是否接受还价、出售剩余时间
- 级别、角色、门派、新版乾元丹数量、月饼粽子机缘、飞升/渡劫/化圣
- 成就点数、已用潜能果数量、总经验
- 攻击修炼、防御修炼、法术修炼、抗法修炼、猎术修炼、育兽术
- 攻击控制力、防御控制力、法术控制力、抗法控制力

### 2. 技能信息
- **师门技能**：技能名称和等级
- **生活技能**：技能名称和等级
- **剧情技能**：技能名称和等级
- **熟练度**：打造熟练度、裁缝熟练度等

### 3. 道具/法宝信息
- **装备列表**：装备名称和描述
- **神器**：神器名称和属性
- **已装备灵宝**：灵宝名称和属性
- **未装备灵宝**：灵宝名称和属性
- **已装备法宝**：法宝名称和属性
- **未装备法宝**：法宝名称和属性
- **货币信息**：现金、存银、储备、善恶、仙玉、精力
- **行囊扩展**：行囊扩展数量

## 数据展示

提取的数据会以友好的方式展示：

- **人物/修炼**：表格形式展示所有基础属性
- **技能**：分类展示（师门技能、生活技能、剧情技能），每个技能显示名称和等级
- **道具/法宝**：分类展示装备、神器、灵宝、法宝，显示数量统计

## 数据保存

所有提取的关键信息会以结构化JSON格式保存到数据库的 `extracted_data` 字段中，格式如下：

```json
{
  "basic_info": {
    "亮点": "...",
    "编号": "...",
    "价格": "...",
    ...
  },
  "skill_info": {
    "school_skills": [
      {"name": "技能名", "level": "等级"}
    ],
    "life_skills": [...],
    "juqing_skills": [...],
    "proficiency": {...}
  },
  "equip_info": {
    "equipments": [...],
    "shenqi": [...],
    "lingbao_equipped": [...],
    "fabao_equipped": [...],
    "currency": {...},
    "bag_expansion": "..."
  }
}
```

## 技术实现

### 核心模块

1. **MultiTabScrapingService** (`src/application/services/multi_tab_scraping_service.py`)
   - 协调多标签页抓取流程
   - 依次点击标签页并提取数据

2. **SkillExtractor** (`src/core/extractors/skill_extractor.py`)
   - 提取技能相关信息

3. **EquipExtractor** (`src/core/extractors/equip_extractor.py`)
   - 提取道具/法宝相关信息

### API端点

- `POST /api/fetch_all_tabs`
  - 请求参数：`{url: string, method: 'playwright'}`
  - 返回：包含所有标签页提取数据的JSON

## 注意事项

1. **仅支持Playwright方法**：多标签页抓取需要页面交互能力，只能使用Playwright
2. **需要登录**：如果页面需要登录，请先点击"自动获取Cookie"按钮
3. **等待时间**：每个标签页点击后会等待1-1.5秒确保内容加载完成
4. **数据完整性**：如果某个标签页提取失败，不会影响其他标签页的数据提取

## 优势

1. **自动化**：无需手动点击，自动完成所有标签页的抓取
2. **结构化**：数据以结构化格式保存，便于后续分析和查询
3. **友好展示**：关键信息以清晰的方式展示，一目了然
4. **节省空间**：只保存关键信息，不保存完整HTML内容

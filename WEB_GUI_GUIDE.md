# Web GUI 使用指南

## 为什么使用Web GUI？

- ✅ **跨平台**：不依赖系统GUI库（如tkinter）
- ✅ **现代化界面**：美观的Web界面
- ✅ **易于使用**：在浏览器中操作，无需安装额外软件
- ✅ **解决macOS兼容性问题**：避免tkinter在macOS上的版本问题

## 启动方法

### 方法1：直接运行

```bash
python web_gui.py
```

### 方法2：使用启动脚本

```bash
./run_web_gui.sh
```

### 方法3：后台运行

```bash
python web_gui.py &
```

## 访问界面

启动后，在浏览器中访问：

```
http://127.0.0.1:5000
```

或

```
http://localhost:5000
```

## 功能说明

### 1. URL输入
- 在"网页地址"输入框中输入要抓取的URL
- 支持 `http://` 和 `https://` 协议
- 按 `Enter` 键可快速开始抓取

### 2. Cookie设置（可选）
- **手动输入**：在Cookie文本框中粘贴从浏览器复制的Cookie
- **从文件加载**：点击"从文件加载Cookie"按钮，选择JSON格式的Cookie文件
- **保存到文件**：点击"保存Cookie到文件"按钮，将当前Cookie保存为JSON文件

### 3. 抓取方法选择
- **Requests + Cookie**：快速方法，适合大多数情况（推荐）
- **Selenium**：支持JavaScript渲染，适合动态页面

### 4. 操作按钮
- **开始抓取**：开始抓取网页数据
- **保存到数据库**：将预览的数据保存到数据库（抓取成功后可用）
- **查看已保存数据**：查看数据库中所有已保存的数据
- **清空预览**：清空当前预览内容

### 5. 数据预览
- **标题预览**：显示抓取到的页面标题
- **内容预览**：显示抓取到的页面正文内容（可滚动查看）

### 6. 状态提示
- 页面顶部会显示操作状态和提示信息
- 成功、错误、信息提示用不同颜色区分

## 使用流程

### 基本流程（无需登录）

1. 启动Web GUI：`python web_gui.py`
2. 在浏览器中打开 `http://127.0.0.1:5000`
3. 在"网页地址"输入框中输入URL
4. 选择抓取方法（默认使用Requests）
5. 点击"开始抓取"按钮（或按Enter键）
6. 等待抓取完成，在预览区域查看数据
7. 确认无误后，点击"保存到数据库"

### 需要登录的页面

1. **获取Cookie**：
   - 在浏览器中打开目标网站并登录
   - 按 `F12` 打开开发者工具
   - 切换到 `Application`（应用程序）标签
   - 左侧选择 `Cookies` -> 选择网站域名
   - 复制所有Cookie的name和value

2. **使用Cookie抓取**：
   - 在Web界面中粘贴Cookie到"Cookie字符串"文本框
   - 输入要抓取的URL
   - 选择抓取方法
   - 点击"开始抓取"

3. **保存Cookie供以后使用**：
   - 输入Cookie后，点击"保存Cookie到文件"
   - 文件会自动下载到浏览器默认下载目录
   - 下次使用时，点击"从文件加载Cookie"即可

## 查看已保存的数据

1. 点击"查看已保存数据"按钮
2. 在弹出的窗口中查看数据列表
3. 点击"查看详情"按钮可查看完整数据（包括URL、标题、内容和创建时间）

## 常见问题

**Q: 启动后无法访问？**  
A: 检查端口5000是否被占用，或修改代码中的端口号

**Q: 如何停止服务器？**  
A: 在终端中按 `Ctrl+C`

**Q: 可以在其他设备上访问吗？**  
A: 可以，但需要修改代码：
```python
app.run(debug=True, host='0.0.0.0', port=5000)  # 允许外部访问
```
然后使用本机IP地址访问，如：`http://192.168.1.100:5000`

**Q: Cookie过期了怎么办？**  
A: 重新从浏览器获取Cookie并更新

**Q: 提示"抓取失败"？**  
A: 
- 检查URL是否正确
- 如果页面需要登录，确保Cookie正确
- 尝试使用Selenium方法
- 检查网络连接

## 优势对比

| 特性 | Web GUI | 桌面GUI (tkinter) |
|------|---------|-------------------|
| 跨平台 | ✅ 是 | ⚠️ 需要tkinter支持 |
| macOS兼容性 | ✅ 完美 | ❌ 可能有版本问题 |
| 界面美观度 | ✅ 现代化 | ⚠️ 传统风格 |
| 安装依赖 | ✅ 只需Flask | ⚠️ 需要系统GUI库 |
| 远程访问 | ✅ 支持 | ❌ 不支持 |

## 技术栈

- **后端**：Flask (Python Web框架)
- **前端**：原生HTML/CSS/JavaScript
- **样式**：现代化渐变设计，响应式布局
